/*
    TODO: Container - mapDispatchToProps - mapStateToProps
    ? container is used to connect react and redux, just like react-redux
    ? connect redux with react
    ? import react-redux, action and components
    ? use mapDispatchToProps
    ? use mapStateToProps
*/

