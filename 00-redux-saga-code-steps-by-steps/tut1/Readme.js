/*
    TODO: Introduction + Redux and saga flow diagram
    ? saga is used to handle async operation handle, handle api data
    ? middleware means = tug or saga
    ? redux is used for state management in react.js
*/