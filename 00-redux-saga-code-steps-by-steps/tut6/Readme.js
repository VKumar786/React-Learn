/*
    TODO: Switch Statement + Get data from redux to Component
    ? useSelector hook is used to carry data from redux store to application
    ? useDispatch hook is used to call function or intialize action
*/